# CANARY SET for scripts/backfill_vector_space.py — the six safety# rows, ruled by the
# architect 2026-09-19 as the first batch.
#
# WHY THESE SIX. They are the rows behind a KNOWN, REPRODUCIBLE SYMPTOM, so the canary has a
# pass condition that is not "the script did not crash":
#
#     before   "what hazards are unattended" -> pool of 1 (product#Part), no_compatible_verbs
#     after    safety#Hazard enters the candidate pool and the verb routes
#
# Hand-scored, the query vector already ranks Hazard (0.659) above Part (0.520) — so if the
# relocation works, this question is the one that visibly changes. Six rows also means a
# mistake costs six rows, and every one of them is re-derivable from the ontology.
#
# Run:
#   kubectl -n sandbox exec -i <engine-o pod> -- python - \
#       --classes OntologyClass --uris-file /dev/stdin --apply --i-have-read-the-warning \
#       < scripts/backfill_vector_space.py
#   ^ the --uris-file must be readable BY THE POD; simplest is to copy this file in first,
#     or run the script from a checkout with a route to the store.
#
# VERIFY AFTER: the script's own nearObject(self) line must say RETRIEVABLE. Do NOT verify by
# looking for a vector on the row — a repaired row reads as having none. That is the repair.
http://internal/sustainment/safety#AcceptanceAuthority
http://internal/sustainment/safety#Hazard
http://internal/sustainment/safety#Mitigation
http://internal/sustainment/safety#RiskAssessment
http://internal/sustainment/safety#SafetyCriticalItem
http://internal/sustainment/safety#WriteUp
