# Night dispatch — overnight 2026-09-17/18

to: ia-01/lane/01
read-by: ia-01/lane/01 2026-09-19

From the architect, relayed by Lane 1 (`invincible-agent-65`, `ia-01`/`lane/01`).

**Standing grants as before: merges on the gate, one roll with hooks near morning, nothing that
needs a browser, no NetworkPolicy, no wipe of anything shared.**

---

## Lane 1

Finish the roll in flight, census with standing gaps **as state**, and the report of what's live
for the morning walks — **safety step 2, the finance board, "how do I add an engine," the lot 3
menu** — in that order.

Then: **the wipe control, once, in an ephemeral namespace** — engine-o's and cortex-bff's
registrations coming back through the hook chain — so the waiver stops being provisional.

**NetworkPolicy allowlist** prepared from the eo census plus live connection reads per substrate
port. **Apply nothing.**

If ca cuts `v0.9.3` before 05:00 the morning roll carries the pin; otherwise it waits for the
next.

## ca

Declare the trace writer's edge types from the eo census's **13/13 partition** — not `None`.

Cut **`v0.9.3`**: exports promoted; `[server]` extra as its own commit **after** the guard's
green run; edge registries; `enumerateInstances` with bound slots and `scoped_by`. Update the
docs status line with the release.

## eo

The **11 unclassified stores**, each with a proposed class, its producer, and an owner, as
packets to the owning lanes.

`MeshGraph`'s Neo4j implementation **of the reads engines actually make**, from your own
inventory. `_ensure_opened` off the alias.

## 7f

The ingest door's declaration in the SDK's form: the stores doc-tools writes — Neo4j
`OntologyClass` nodes, the Weaviate collections, the eight edge types — each `rebuildable` with
doc-tools as producer. The conformance seal **in doc-tools' repo** against that declaration.

The `MeshCollectionMeta` writer on `v0.9.2`.

## 5f

The **ADR-0054 enforcement packet** now that the census exists: the clearer reading the class,
the census gate at zero per environment, the three-state refusal, **sequenced label-first**.

`adding-an-engine.md` rewritten from engine-docs' own build as the worked walkthrough, with the
sixteen sites and the seals that name each. A runbook for **reading a census**.

**`docs_agent` corrected on master — three things, none of which your branch could have seen:**

- **Pinned to the fleet's `v0.9.2`** (it was at `v0.9.1`, the fleet pin on the day it was
  written, merged after the fleet moved). The pyproject's stated reason for `v0.9.1` — *"the
  release whose Protocol declares `page_for_subject`"* — **was false and is removed**; a
  correction further down that same file already recorded that `page_for_subject` is declared in
  the engine's own `reads.py` and at no SDK version. Two contradicting claims in one file, the
  wrong one load-bearing. Verified at the consumer: `_FASTAPI_MISSING` still at 4 occurrences.
- **`endpoint_url` per verb**, derived from the registrar's loop variable. It was the literal
  `f"{base}/explain"`, which is correct today and only because there is one verb — the registrar
  bakes one URL per verb, so the second verb would have registered, reported accepted, and
  answered with its sibling's handler.
- **`domains=[]` explicit**, with its reason and its retiring condition. I first put this on the
  scope-exclusion list; the architect ruled it belongs at the CALL, under Tuesday's registration
  amendment — agnostic is declared, never reached by an omitted argument, because the omission is
  the gate inverted by a shorter call. The census's agnostic line now shows it as **chosen**
  rather than as forgotten.

## 74

The persistence writer with its store's class declared under **ADR-0054**.

`assessDeferralRisk` through `mro:MaintenanceWorkOrder` once the `iof_mro.ttl` manifest row
primes — **confirm with Lane 1 whether it did.**

## 91

**Rebase `lane/91` onto master**, resolve the four conflicts, push. `scope_label` per row. The 18
mirror rows routed through the register.

See `sessions/2026-09-17-dispatch-lane-91-rebases-onto-master.md` for what conflicts and what
moved under you. Short version: the runtime import you were needed for (`7b97279`) is **already
on master**, nothing of yours the roll needed is missing, and `cost_agent/measures.py` is a
semantic conflict in your own finance body that nobody outside the lane should resolve.

## 32

The **BRIEF artifact** with `unsummarised` rows and artifact links, on the fleet, so cortex-60
has a real one. The second-process resume seal green **against the real database**. The
`timed_out` graph.

## cortex-60

BRIEF from 32's artifact **when it exists**. The general `UnreadFields` surface in Detailed mode,
reporting never rendering. `flags` as flags, so the producer can narrow.

---

## Morning — the architect

The four walks, in Lane 1's order, then whatever the census names.

**Final report** at the end of the window: one document a stranger could act on, with the
three-item lists updated as its last section.
