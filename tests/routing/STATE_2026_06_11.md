# Substrate + routing state as of 2026-06-11 (overnight session)

Picks up from the morning-handoff sequence (routing-baseline-v1 →
phases). This document is the end-state snapshot — what's running,
what's reproducible, what's open.

## End-state — substrate

### Neo4j (`OntologyClass` nodes + verb edges)

- 6 idp:* full-IRI classes present: Dataset, Table, Dashboard, Column,
  Pipeline, Job. Hierarchy: Table + Dashboard `subClassOf idp:Dataset`.
- 9 catalog verbs typed against `idp:Dataset` as input:
  - `mesh:assessImpact`, `checkFreshness`, `describeAsset`,
    `filterByTag`, `findSchema`, `lookupOwnership`, `traceLineage`
    (formerly typed against `mesh:CatalogAssetQuery`)
  - `mesh:enumerateCatalog` (formerly `mesh:CatalogScopeQuery`)
  - `mesh:analyzeDataset` (formerly `mesh:DatasetAnalysisRequest`)
- 2 mro verbs still typed against `mro:WorkInstruction` + `mro:TechnicalManual`
  (from the earlier mro migration).
- 1 remaining Contract D debt: `mesh:analyzeWithCodeAgent` against
  `mesh:AgentTask` (not catalog; needs its own subject decision).
- Pseudo-class nodes (`mesh:CatalogAssetQuery`,
  `mesh:CatalogScopeQuery`, `mesh:DatasetAnalysisRequest`) still
  exist as nodes — orphan in terms of verb edges, kept because of
  residual `subClassOf` bridges from legacy compact-form classes.
- All 8 substrate invariants in `test_substrate_invariants.py` pass.
  `PSEUDO_FIXED` now includes the 3 catalog pseudo-classes; only
  `mesh:AgentTask` remains in `PSEUDO_KNOWN_DEBT`.

### Weaviate (resolver candidate pool)

- `MAINTENANCE` domain: clean. `mro:*` and `data:*` retired; the 20
  mesh:* full-IRI entries that were mistakenly ingested at MAINTENANCE
  (system internals polluting user-facing pool) were deleted.
- `DATA_ENGINEERING` domain: only `idp:Dataset` and `idp:Dashboard`.
  - `data:Dataset` and `data:Dashboard` (hand-curated mystery-seeded
    classes) retired — catalog queries now resolve to the canonical
    full-IRI `idp:Dataset` form.
  - `idp:Table`, `idp:Column`, `idp:Pipeline`, `idp:Job` retired from
    Weaviate **temporarily** — they polluted the catalog candidate
    pool without Phase 5 verb coverage (the LLM correctly refused
    them because verbs are typed against `idp:Dataset`, not the
    subclasses; subClassOf doesn't carry into the LLM's constrained
    enum reasoning). They remain in Neo4j and in the canonical TTL
    source. They get re-added in the same coordinated change as
    Wave-3 verb migration to those subjects.

### Jena + MinIO (canonical record)

- `s3://ontologies/idp/idp_extension.ttl` — 6 idp:* classes, verbatim
  definition transcription from the old hand-curated `data:*` classes
  (for Dataset and Dashboard); fresh definitions for Column, Pipeline,
  Job. Ingested via canonical pipeline with explicit
  `extra_metadata.domain=DATA_ENGINEERING`.
- `s3://ontologies/mesh/mesh_system.ttl` — present, was re-ingested
  at `MAINTENANCE` in error; the Weaviate side was cleaned but the
  Jena named graph still has it. Worth re-ingesting at
  `explicit_domain=MESH` to keep canonical reproducibility without
  contaminating user domains.

## End-state — services

### iagent-mesh-registrar (NEW)

- Deployed via helm `--set meshRegistrar.enabled=true`. Pod healthy,
  service at `iagent-mesh-registrar:8090`.
- Smoke-tested:
  - `/health` returns OK with `neo4j_reachable: true`.
  - `/v1/register` rejects fake URIs at HTTP 422 with the specific
    `missing` list (Contract D enforcement).
  - `/v1/register` accepts real URIs and emits a `mlModelProperties`
    MCP to DataHub GMS (`status: registered`, DataHub `ok: true`).
- SDK companion (`iagent_mesh.core._emit_to_registrar`) is committed
  but engines haven't been re-deployed to pick up. Each engine opts
  in by setting `MESH_REGISTRAR_URL=http://iagent-mesh-registrar:8090`
  on its deployment; until set, engines use the legacy direct DataHub
  path.

### iagent-engine-o (UPDATED)

- Image rebuild from `c8cd315` (llm_utils temperature 0 + OLLAMA_MODEL
  preference). The runtime BAML Ollama-client registration in
  `init_baml_client` was silently overriding clients.baml's
  `temperature 0`; this fix sets it explicitly on the registry
  registration so the constrained-enum classifications are
  deterministic. Engine-o rolled to this image; matrix re-run in
  flight to confirm.

## Matrix state

Last verified: 54/55 across 5 runs at 155-165s per run (= routing-
baseline-v1 latency). The 1 flake was rotor `verb_confidence=0.00` —
the init_baml_client temperature bypass that c8cd315 fixes. Re-run
post-engine-o-rebuild is in background task `borgfjfb3`; expectation
is 55/55 deterministic.

## What's reproducible from canonical source now

- All mro:* + mesh:* MAINTENANCE classes via `s3://ontologies/mro/{mro_extension,maintenance_extension}.ttl` (last night) and `s3://ontologies/mesh/mesh_system.ttl` (tonight, needs domain-override re-ingest)
- All idp:* DATA_ENGINEERING classes via `s3://ontologies/idp/idp_extension.ttl`
- 11 catalog + mro verbs via `scripts/phase5_catalog_verb_migration.py`
  + the earlier mro verb-retyping scripts
- mesh-registrar gateway via the helm chart + invincible-agent CI

## What's still hand-seeded (not yet reproducible)

- The 30 OWL blank-node entries (`n68aaf338*`) in DATA_ENGINEERING
  from rdflib's PROV-O parsing — these are pipeline-natural noise,
  not mystery seeds. They reproduce automatically when PROV-O.ttl
  is re-ingested.
- Residual `subClassOf` bridges from the original compact-form
  legacy seed connecting `mro:Procedure/Symptom/Equipment` etc. to
  `mesh:GraphQuery` — orphan but harmless until those subjects get
  matrix coverage.

## Open follow-ups

1. **Compliance plugin URI prefixed-name bug** (caught by the
   doc-tools integration tests in `4c1aac6`/`a985951`): rule_node_id
   is built from `manual_reference` with only word-char replacements,
   leaking quotes/parens into the SPARQL URI prefixed name → HTTP 400
   from Fuseki. Fix: add a URI-fragment sanitizer.

2. **mesh_system.ttl proper home**: re-ingest at
   `extra_metadata.domain=MESH` so the canonical source still
   reproduces without polluting MAINTENANCE.

3. **Wave-3 verb migration**: type column-lineage / pipeline-source /
   job-status verbs against idp:Column / idp:Pipeline / idp:Job, then
   re-add those classes to Weaviate (still in Neo4j + TTL).

4. **Engines opt into mesh-registrar**: set `MESH_REGISTRAR_URL` on
   each engine deployment via the chart values, drop `acryl-datahub`
   from per-engine pyproject.toml.

5. **DATAHUB_GMS_URL configmap fix**: was `http://...:8080/api/graphql`
   (the GraphQL endpoint URL), patched to `http://...:8080` (base).
   Verify other consumers (doc-tools sensor, engine-o) aren't broken
   by the change.
