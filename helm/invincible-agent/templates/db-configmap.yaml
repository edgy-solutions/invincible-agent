apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-db-schema
  labels:
    {{- include "invincible-agent.labels" . | nindent 4 }}
data:
  create_bpmn_catalog.sql: |
{{ .Files.Get "sql/create_bpmn_catalog.sql" | indent 4 }}
  create_answer_artifact_projection.sql: |
{{ .Files.Get "sql/create_answer_artifact_projection.sql" | indent 4 }}
